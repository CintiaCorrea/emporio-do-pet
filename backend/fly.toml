# =====================================================
# Fly.io Configuration - Empório do Pet Backend
# =====================================================
# Deploy: fly deploy
# Logs: fly logs
# SSH: fly ssh console
# =====================================================

app = "emporio-pet-api"
primary_region = "gru"  # São Paulo, Brasil

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "8080"
  AI_SERVICE_URL = "http://emporio-pet-ai.internal:8080"
  FRONTEND_URL = "https://emporiodopet.com.br"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [http_service.concurrency]
    type = "connections"
    hard_limit = 100
    soft_limit = 80

[[http_service.checks]]
  grace_period = "30s"
  interval = "30s"
  method = "GET"
  timeout = "10s"
  path = "/api/health"

# Release command - sync schema to database
# Usando db push para criar/sincronizar schema (ideal para banco novo ou sem histórico de migrations)
[deploy]
  release_command = "npx prisma db push --accept-data-loss"

# Machine configuration
[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

